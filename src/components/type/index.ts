export interface InputTypeButton {
  buttonName: string;
  loading: boolean;
}

export interface InputTyPeText {
  placeholder: string;
  type: string;
}

export interface LoadingType {
  loading: boolean;
}